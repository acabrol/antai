{
  "name": "antai",
  "version": "0.1.0",
  "private": true,
  "description": "Antkeeping companion app with a local Node server and repository-backed datastore.",
  "license": "MIT",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check public/app.js && node --check src/server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git"
  }
}
